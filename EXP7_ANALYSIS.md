# Анализ эксперимента exp_7 (3.1M шагов)

> Данные: `models/exp_7/meta.json`, `models/exp_7/best_model.meta.json`, лог обучения из чата,
> код `src/earth.cpp`, `src/env.cpp`, `configs/bases.json` (ветка master, 9f9443f).

## 1. Что было запущено

| Параметр | Значение | Комментарий |
|---|---|---|
| Шаги | 3 145 728 (план 3 000 000) | 8 217 эпизодов |
| Сеть | MLP [256, 256], obs_mode=hybrid | 246-мерный flat + миникарта |
| PPO | lr=3e-4, γ=0.99, λ=0.98, clip=0.2, ent_coef=0.05, n_epochs=5 | γ ниже дефолта v2 (0.995) |
| Среда | n_envs=64, map_size=100, difficulty=normal, курикулум stage 3 | target_kl=0.02, AMP выкл |
| batch | n_steps=4096 × 64 env = 262 144, minibatch 8192 | 32 мини-батча на эпоху |

**Итог:** best eval score = **120.85** (days=1096, bases=6, people≈52.5) на шаге 2 621 440;
средний reward за эпизод best_reward=3.81; FPS 18.5k → 11k к концу; GPU ~7.1 GB.

## 2. Главная проблема: прогон шёл на наградах v1

`meta.json` → reward-секция отличается от `configs/reward_v2.json` в **6 ключах**:

| Ключ | exp_7 (v1) | v2 (проверен свипами) | Эффект v1 |
|---|---|---|---|
| build_bonus | 1.0 | 2.0 | стройка недооценена |
| novelty | **15.0** | 5.0 | ×3 перекачка в «новый тип здания» |
| diversity_bonus | **8.0** | 3.0 | ×2.7 перекачка в разнообразие |
| idle_build_penalty | **−10.0** | −2.0 | ×5 давление «строй каждый день» |
| idle_build_threshold_days | **3** | 7 | наказание наступает вдвое быстрее |
| sale_bonus | 0.2 | 0.5 | торговля недооценена |

Комбинация v1 = агенту выгоднее **спамить разнотипные здания** (novelty+diversity − idle-штраф),
чем выстраивать устойчивую экономику под налоги. Это прямо согласуется с поведением в логе:
базы появляются (6 к best-моменту), но экономика не вытягивает налоговые платежи.

**Почему так вышло:** фикс «награды всегда восстанавливаются из конфига UI» честно загрузил
СТАРОЕ сохранённое состояние UI, где лежали v1-значения. Это не баг фикса, а устаревший state-файл.
**Лечение:** в UI 2.0 добавлена кнопка **«Сбросить к дефолтам v2»** (таб «Награды») — один клик
возвращает весь профиль из `rl/config.py`.

## 3. Смерти от налогов — главный паттерн логa

Эпизоды обрываются на **365 / 731 / 1461 / 2401 / 3897 дней** — это годовые налоговые точки
(365×k ± сдвиг). Колония доживает до сбора налога без кассового резерва → `tax_fail` → game over.

Причины в порядке вклада:
1. **v1-награды** (см. §2): стимул «здание сегодня» сильнее стимула «деньги на налог в декабре».
2. **γ=0.99**: горизонт эффективного планирования ~100 шагов; налог через 300+ дней
   дисконтируется до пренебрежимого сигнала. Дефолт v2 — 0.995 (~200 шагов), для годовой
   экономики лучше 0.995+.
3. Налоговая смерть — единственный стабильный «убийца», но `game_over_penalty` в v1-прогоне
   получает кредит лишь за ~100 шагов до события.

## 4. Огромная дисперсия eval — водный фактор карты (подтверждено кодом)

CI95 по 20 eval-эпизодам: **days [365 … 2401]**, bases [4.5 … 21.1], people [29 … 97].
Разброс ×6.6 — это не шум политики, это **разные карты**:

- `src/earth.cpp`: 2–5 озёр радиуса 6–16 в случайных позициях `[10, size−11]`;
- `configs/bases.json`: `WaterChannel` имеет `need_earth: 2` (LT_WATER) — водоканал ставится
  **только на воде**;
- `src/env.cpp::find_lot`: доступный лот ищется BFS от колонии по дорогам/зданию —
  если озеро не связано с центром, воды нет → нет пищи → население вымирает независимо
  от качества политики.

При `map_size=100` и 2–5 озёрах вероятность «сухой» карты существенна. Отсюда:
- «плохие» eval-эпизоды упираются в потолок 365 дней (первая годовая точка);
- «хорошие» доживают до 2401+.

**Что сделано в этой сессии:** `watch_champion.py` теперь по умолчанию берёт **случайный seed
карты** на каждый запуск и печатает его (`Random map seed: N (re-watch … --seed N)`), так что
наблюдение больше не зациклено на одной (возможно, непроходимой) карте. В UI 2.0 seed карты —
отдельный контрол с 🎲 и чекбоксом «случайный при старте».

**Рекомендация:** для честного сравнения прогонов фиксировать набор eval-seeds
(`eval_seeds` в Config) либо поднимать `eval_episodes`, чтобы карты усреднялись.

## 5. Прочие наблюдения

- **FPS 18.5k → 11k.** Колония растёт: больше зданий/людей → дороже C++-шаг (обход зданий,
  логистика, налоги). Деградация плавная, без скачков — утечек/аномалий не видно.
- **best_reward=3.81 (средний per-episode reward).** Эпизоды в среднем чуть «в плюсе»:
  агент научился собирать мелкие бонусы (novelty/diversity из v1), но не выживать.
- **best на 2.6M из 3.1M, кривая score ещё росла.** Прогон обрезан по бюджету, а не сошёлся —
  продление до 6M на v2-наградах оправдано.
- **Энтропия не схлопнулась** (ent_coef=0.05 достаточен) — исследование не было узким местом.
- **Краш наблюдения** (`AttributeError: tax_fail_penalty`, cpp_env.py:135) — старый .pyd без
  9 новых reward-полей. Исправлено: hasattr-гард + WARNING со списком неприменённых ключей
  вместо краша; для фактического применения ключей всё равно нужен ребилд `build_pyext.bat`.

## 6. Итоговые рекомендации для следующего прогона

1. **Награды v2** (кнопка сброса в UI 2.0 или `configs/reward_v2.json`).
2. **γ=0.995** — горизонт под годовой налоговый цикл.
3. **6M шагов**: best exp_7 пришёлся на 2.6M при растущей кривой.
4. **Фиксированные eval-seeds** (или ≥40 eval-эпизодов) — иначе CI95 дней [365…2401]
   делает сравнение прогонов бессмысленным.
5. Оставить map_size=100 для скорости, но финальную валидацию делать на 280 (боевой размер) —
   связность озёр на 100×100 не репрезентативна.
6. Ребилд .pyd после reward-изменений (build_pyext.bat), иначе 9 ключей молча останутся
   на C++-дефолтах (теперь хотя бы с WARNING в логе).
